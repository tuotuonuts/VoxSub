# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

> 记录为 `web` 的依据：Electron 是 Chromium 渲染 + Web 技术栈，视觉与实现语言是 web；
> 目标平台近期仅 Windows，浮窗依赖的 `WDA_EXCLUDEFROMCAPTURE` 为 Windows 10 2004+ 独有。
> 跨 macOS 时该能力需改用 `CGWindowSetSharingType`，效果不等价 —— 已确认近期不做。

## Stack

Electron + TypeScript 前端，IPC 拉起现有 Python 后端（`voxsub` 包，26000 行，440 个测试全绿）。
用户明确选择：后端一行不改，只换 UI 层，一次只改一层。

- 渲染：Electron（Chromium）
- 前端语言：TypeScript
- 后端：现有 Python 包，作为独立进程经 IPC 调用
- 打包：PyInstaller（后端 sidecar） + electron-builder（外壳）

## Users

**主要用户：需要跨语言获取内容，但没有技术背景的 Windows 普通用户。**

三个具体场景，按使用频率排序：

1. **听英文会议 / 网课** —— 内容实时流出，过去即失，无法回看；需要在听的同时读到中文。
2. **看无字幕或生肉的视频** —— 有画面、有声音，但没有可读的文本。
3. **跨境工作 / 与外方沟通** —— 需要即时理解对方在说什么，而不是事后查词典。

共同特征：**不是设计师，不是开发者**。不会为了配置一个模型去读文档。默认全本地离线运行
是一条产品底线，不是宣传语 —— 用户不认识「端到端加密」「本地推理」这些词，他们只是
不想把自己的会议内容传出去。

次要用户（已确认存在，但非设计目标）：需要批量把音视频转成双语字幕的轻度创作者。

## Product Purpose

把「听不懂的话」在**说完的瞬间**变成「看得懂的字」。

四类入口覆盖同一个能力：麦克风、系统声音、音视频文件、屏幕截图。
成功 = 用户不再需要停下来查、不再需要等字幕组、不再需要第二台设备。

## Positioning

**说一句翻一句，纯本地，零配置可用。**

邻居产品做不到的两件事，同时做到：

- 云翻译工具（DeepL / 各家会议字幕）：效果可以更好，但**做不到本地**——音频必须上传。
- 本地开源工具（各种 whisper 前端）：可以本地，但**做不到零配置**——用户要自己下模型、
  自己调参数、自己接管线，且多是「说完等一会儿出全文」而非边说边出。

本产品的机制是 **句子级流式**：不等整段结束，说完一句即刻出双语。
配合「模型广场 + 硬件自动路由」把配置成本压到接近零：用户点一下，剩下的自己选。

## Operating Context

**使用环境（决定了明暗与对比度的取法）：**

- 开会时：用户在自己的工位上，很可能同时开着会议软件、共享着屏幕。
- 看视频时：全屏播放器占满屏幕，字幕浮窗压在画面之上。
- 网课时：长时间（一节课 45 分钟起）盯着字幕区。
- 屏幕翻译时：原本的界面是别人的产品（网页 / 游戏 / 桌面应用），译文要原位覆盖上去。

**关键环境事实：** 字幕浮窗会长期压在**别的应用的画面上**，且用户可能正在
**共享自己的屏幕**。这两条是浮窗设计的事实约束，不是风格偏好。

**工作流：** 选模式 → 选输入源 → 开始 → 读字幕 → 结束导出。
设置与模型管理是低频动作，不应该挡在主动线上。

## Capabilities and Constraints

四类模式与已确认的功能面（全部需在新前端实现）：

- **A 麦克风同传**：设备选择、分句识别、双语字幕、可选同传录音（开始/暂停/继续/结束并保存）、可选 TTS 朗读
- **B 系统声音 / 指定应用**：系统输出端点选择，或按进程树捕获指定应用（Windows 10 2004+）
- **C 音视频文件**：导入 MP4/MKV/MOV/MP3/WAV 等，内置 ffmpeg 提取音频，导出 SRT/VTT/TXT
- **D 屏幕 OCR**：框选截图、上传图片（PNG/JPG/WebP/BMP/TIFF）、原位覆盖译文、实时区域、
  译后图导出、原图与译后图分离缓存（强制非 C 盘，默认各留 15 张）
- **模型广场**：18 个模型（翻译 7 / OCR 4 / ASR 4 / TTS 3），按质量排序、硬件推荐标注、
  双源下载（ModelScope 主源 + GitHub/HF）、断点续传、SHA256 校验、切换与卸载
- **诊断**：自检 6 项（模型完整性 / ORT providers / ASR 冒烟 / VAD 冒烟 / TTS 冒烟 / 磁盘内存）、
  实时日志、报告导出
- **字幕浮窗**：半透明置顶、双语两行、拖动、缩放、锁定与鼠标穿透、
  显示模式（原文/译文/对照）、字号、透明度、内容边距、行距
- **识别调优**：5 种预设（自动/响应优先/均衡/准确优先/自定义）+ 智能上下文；
  可调 VAD 灵敏度、静音断句、最长等待、单句时长、候选数、最大文字量、hotwords；
  智能上下文额外有实时双语草稿、保守纠偏、语气词清理
- **本地 / 云混合**：STT 与翻译独立选择，四条链路（本地+本地 / 云+本地 / 本地+云 / 云+云）
- **会话**：双语历史滚动、清空、导出 TXT/SRT/VTT、文字可选中复制
- **托盘**：模式切换、开机自启、退出
- **主题**：深色 / 浅色 / 跟随系统；UI 语言简中 / English 即时切换

**技术约束：**

- 推理栈固定：onnxruntime（DirectML 提供 GPU 加速）+ sherpa-onnx（ASR/VAD/TTS）+ RapidOCR
- 硬件路由优先级：独显 GPU → NPU → 核显 → CPU；不支持时显式降级，**禁止伪报加速**
- 音频不落盘（除用户显式开启同传录音）；截图像素只在内存处理
- 新增队列必须有容量上限；正式文件写入必须原子提交

**明确未决（不臆造）：**

- 是否替换现有 VoxSub 的 UI 层，还是并行演进 —— 用户要求在看到原型后再定
- 跨 macOS 的时间点 —— 近期不考虑

## Brand Commitments

- **产品名：语幕 VoxSub**（中文主打「语幕」，英文名 VoxSub）。英文名在 GitHub 有同名同类
  项目，已评估无商标风险，保留。
- 中文名「语幕」的意象：**语 = 语言 / 说话，幕 = 屏幕 / 字幕幕布**。
  这是已确认的命名意图，新前端应当能支撑这个意象，而不是背离它。
- 产品语气：直接、不夸张。不承诺「媲美人工翻译」，不把「猜的设备能力」写成「已验证」。
  现有 README 与更新日志整体是这个语气，保持。
- 无 logo / 视觉识别资产待继承（现有图标为程序化生成的占位，可重新设计）。

## Evidence on Hand

**已有的真实资产：**

- 完整可用的 Python 后端（`voxsub/`，26000 行，440 测试全绿）—— 真实能力，非演示
- 4 个模式 + 18 个模型的真实实现，含真实模型下载与硬件路由
- 真实的性能数据：0.9.0 候选安装包 264 MB；本机启动 15 秒（其中硬件探测占 5 秒）
- 已发布的公开版本 v0.7.2-beta（GitHub），后续版本为未发布候选
- 一份可用的设计令牌表（深/浅两档）—— 但**未经验证是否足够好看**，可推翻

**明确没有、不得编造的：**

- **没有真实用户量、留存、满意度数据** —— 目前是内测阶段，不得写进任何对外文案。
- **没有商业数据**（价格、客户、营收），不编造 testimonials 或 customer logos。
- **没有品牌视觉资产**（logo、字体授权、色板）—— 需要时重新建立，不得虚构「已有品牌系统」。
- **没有跨平台验证** —— 只在 Windows 上跑过。

## Product Principles

1. **说完即见。** 延迟是这个产品的核心指标。任何为效果牺牲「说完即出」的取舍都要先被质疑。
2. **默认可用，配置可选。** 一个不懂模型的用户应该能直接开始用。所有专业参数默认隐藏，
   需要时才展开，且每项要用非技术语言解释。
3. **内容不出本机。** 本地优先不是功能，是承诺。任何需要上传的路径必须是用户显式选择，
   且要在界面上说清「什么会被送出去」。
4. **不假装。** 设备不支持就说支持不了，模型没验证就说未验证，识别错了就显示原文。
   这个产品面向的是「听不懂就想看懂」的人，骗他们一次就永远失去他们。
5. **字幕要能一直看。** 用户可能连续盯一小时。这意味着对比度、字号、动态范围都要按
   「长时间阅读」的标准取，而不是按「截图好看」的标准取。
