# 语幕 VoxSub — 发布说明 (RELEASE_NOTES)

> [!WARNING]
> VoxSub 尚未成熟，当前版本均为开发测试版，不应依赖它处理关键工作。`0.7.2-beta` 使用 `CN=VoxSub Dev (self-signed)` 开发者自签名，但证书不受 Windows 公共信任链信任；SmartScreen 或杀毒软件仍可能出现风险提示或误报。请仅从本仓库 Releases 下载并核对 SHA256，不建议关闭安全软件绕过警告。

## 版本: v0.9.0-beta（开发候选，未发布）

这一版把 OCR 从首个候选功能补全为可上传、可缓存、可导出、可切换模型的图片与屏幕翻译工作流，同时修复安装包内 OCR 完全不可用的问题。

### 实时性能与 GPU

- 日志与真机基准确认，翻译使用的 Hy-MT2 已运行在 RTX 4060 / Vulkan；此前持续占用 CPU 的是 RapidOCR，因为 OCR 会话没有配置已随包安装的 DirectML provider。现在截图和实时 OCR 会优先创建 DirectML GPU 会话，界面与日志显示实际 provider；GPU 初始化或单帧推理失败时会立即重建 CPU 会话并重试，不会让整个 OCR 功能失效。
- 实时模式不再执行通常不需要的文字方向分类；没有可用 GPU 且选择 Medium/Server 质量档时，实时区域会临时使用内置 Small 保证响应，单张图片仍保留用户选择的高质量模型。
- OCR 译文从“每一行一次模型请求”改为最多 12 行、900 字符的有界批量请求；严格校验 JSON 数量与顺序，异常时才逐行回退。相同文字继续复用有界缓存，页面只改变少量区域时不会重新翻译所有行。
- 本机 1920×1080、30 行 PP-OCRv6 Medium 基准：原 CPU 热帧约 24 秒，DirectML 首帧（含模型初始化）约 2.95 秒，后续热帧约 0.45 秒。该数字用于回归对比，不承诺所有显卡达到同一耗时。

### 故障与视觉修复

- 根因日志确认 `rapidocr` 通过模块级延迟导出 `RapidOCR`，PyInstaller 没有发现真实的 `rapidocr.main`。运行时现改为直接导入实现模块，构建参数显式添加 hidden import，并把 `VoxSub.exe --ocr-smoke` 设为每次打包必过门禁。
- 用户安装目录进一步确认残留了旧版不完整的 `_internal/brotlicffi`，覆盖安装只写入新文件但不会删除已移除依赖，最终让 urllib3 在 RapidOCR 初始化时访问不存在的 `brotlicffi.error`。升级安装现会在关闭 VoxSub 后清理 `_internal`、`tools` 和 `models_base` 三个应用管理目录再写入当前运行时，明确不触碰 `Models` 与 `Cache`。
- 实时区域 OCR 遇到引擎/模型不可用会立即暂停，不再每 700ms 重试并持续刷日志。
- OCR 入口从左侧模式卡堆栈移到顶部工具区，A/B/C 模式选择不再被新按钮挤压。
- 框选前会先处理 Qt 隐藏事件、等待 Windows DWM 合成完成，再额外保留稳定间隔；冻结的桌面背景不再包含主应用虚影。

### 图片上传、覆盖与导出

- 截图页新增图片上传，支持 PNG、JPG、JPEG、BMP、WebP、TIF/TIFF。
- 同一识别结果可在“原图 + OCR 框”和“原图位置覆盖译文”之间切换；覆盖算法与实时区域 OCR 共用绘制实现，避免两套效果逐步分叉。
- 译后覆盖图可导出为 PNG 或 JPEG，写入过程使用 Qt 原子保存文件。

### 非 C 盘缓存

- 上传/截图原图保存在 `Cache/OCR/originals`，未导出的译后图保存在 `Cache/OCR/translated`，两类文件不会混放。
- 缓存路径在写入前强制解析并拒绝 C 盘；默认位于非 C 盘应用目录。如果应用在 C 盘，会优先使用非 C 盘模型目录的相邻 Cache；仍无合规路径时要求用户在设置中选择，不会静默写入系统盘。
- 设置 → 存储与模型可更改 OCR 缓存位置，并设置每类保留数量：默认 15，最少 1，0 表示无限。缩小上限后会立即分别整理两类缓存。

### OCR 模型广场

- 新增 OCR 分类和四档可切换运行时：内置 PP-OCRv6 Small、低延迟 Tiny、高精度 Medium、PP-OCRv5 Server 文档/手写增强档。
- 下载文件使用 RapidOCR 3.9.2 官方 ModelScope 地址与 SHA256；模型卡只有文件完整时才能设为使用，OCR worker 会在模型 ID 变化时重建引擎。
- PP-OCRv5 档针对复杂中英文手写、竖排和生僻字增强；艺术字仍受字形清晰度限制，界面不做不现实的“全艺术体保证”。

### 验证与发布物

- 加速与批量翻译源码全量门禁为 `294 passed, 8 skipped`；复杂度预算、DirectML provider、批量顺序/缓存和 OCR UI 回归均通过。
- 打包后的 `VoxSub.exe --ocr-smoke` 独立复跑退出码为 0，隔离日志确认 `backend=GPU · DirectML`，成品目录包含 `DirectML.dll`。
- 候选安装包 `VoxSub-Setup-0.9.0-beta.exe` 为 277,338,824 字节（264.49 MiB），SHA256 `9B6D57572E1A2C17178C2A169587DD57CA312C265D9EF343A8D3AD6B3334ED6B`；当前构建环境没有可用代码签名证书，主程序与安装包未签名。
- `0.9.0-beta` 仅推送源码与安装包，不在用户验证前创建 GitHub Release。

## 版本: v0.8.0-beta（开发候选，未发布）

这一版新增独立的 OCR 屏幕翻译工作区，不改变 A/B/C 语音与文件字幕模式。

### 两种 OCR 模式

- “截图 OCR 翻译”采用跨显示器框选，完成一次识别与翻译后显示截图坐标框、原文和译文，并可分别复制。
- “实时区域 OCR”持续观察选定的游戏、视频、网页或应用区域，只在画面明显变化后重新识别；译文按照识别行坐标覆盖原文位置。
- 独立悬浮控制条支持暂停/继续、临时显示原文/译文、重选区域和结束；主窗口在框选和实时覆盖期间不会挡住目标内容。

### 性能、隐私与架构

- 内置 RapidOCR 通用印刷体模型；截图像素只在本机内存中识别，永不落盘或上传。只有选择云翻译时，识别后的文字会交给用户配置的翻译服务。
- 实时模式每 700ms 做一次低成本画面指纹检查，变化不足阈值时不运行 OCR；任意时刻只有一个 OCR/翻译任务，不积压过期画面。
- Windows 覆盖窗使用捕获排除，失败时截图前短暂隐藏，避免把译文再次识别形成反馈回路；翻译失败会保留 OCR 原文。
- OCR 数据与后端适配、屏幕采集、覆盖绘制、页面编排和单所有者 worker 分属独立模块；后续手写体、艺术字或竖排模型可以替换 OCR 后端，不需要重写 UI。

### 验证与发布物

- 全量门禁为 `276 passed, 8 skipped`；唯一 warning 是既有声卡闭环测试报告一次录音数据 discontinuity，该测试通过。
- 真实 Windows 高 DPI 截图识别和覆盖排除冒烟通过；打包后的 `VoxSub.exe --ocr-smoke` 从成品路径加载三段 ONNX 模型并返回 0；隔离配置启动确认 UI/core 版本和 OCR 页面后，专用安装器事件安全退出，退出码为 0。
- 候选安装包 `VoxSub-Setup-0.8.0-beta.exe` 为 277,266,873 字节（264.42 MiB），SHA256 `04997F7A48B5EB878C4303F6BCCAEC5A72CDD82864131DC5E994FB03AE8E1365`。当前没有可用代码签名证书，安装包为未签名；用户验证前不创建 GitHub Release。

## 版本: v0.7.2-beta（2026-08-24，已发布）

这是安装升级收尾专项修复，解决安装器先假死约半分钟、然后仍报无法自动关闭应用的问题。

### 安装器关闭协议

- 根因是 VoxSub 把普通关窗解释为“隐藏到托盘”，而 Inno Setup 默认 Restart Manager 会等待进程退出，最终在 Windows 长超时后失败。
- 新版应用创建命名退出事件和运行标记；安装器发出专用信号后，会走应用级退出及 Pipeline/下载收尾，不触发主窗的托盘隐藏语义。
- 安全收尾宽限为 2.5 秒；对不支持协议的 `0.7.1-beta` 及更旧版本，或无响应的新版，仅对 `VoxSub.exe` 执行快速进程树关闭，一并收尾 llama-server 等子进程。
- 显式关闭 Inno Setup 的 `CloseApplications` 和 `RestartApplications` 默认等待，无论是旧版兼容路径还是新版协议都不会再卡住约半分钟。

### 应用退出上限

- Pipeline 过去对每条工作线程分别最多等待 8 秒，多线程下可叠加成半分钟以上；现在改为所有线程共享一个 8 秒总截止时间。
- 用户从托盘退出与安装器请求共用同一应用级收尾函数，避免之后两条路径再次分叉。

### 验证与发布物

- 正式构建门禁为 `265 passed, 7 skipped`；新增 Windows 命名事件实测、共享线程截止时间与安装器静态门禁。唯一 warning 为声卡闭环测试的录音数据 discontinuity，该测试通过。
- Inno Setup 7 完整编译通过。安装包 `VoxSub-Setup-0.7.2-beta.exe` 为 214,817,504 字节（204.87 MiB），SHA256 `313714AE3C9557B88EDBCEBBFCB768A15BBDD65915A5266E0B3EB1D82CAF2211`。
- 主程序与安装包均使用 `CN=VoxSub Dev (self-signed)` 开发者自签名；签名状态为 `UnknownError`，不代表 Windows 信任或正式 OV 签名。
- 打包后的真实可执行文件使用隔离配置启动，日志确认 `ui=0.7.2-beta core=0.7.2-beta` 并进入事件循环；专用安装器事件发出后立即安全退出，退出码为 0。测试未触碰当前已安装并运行的旧版进程。
- 标签 `v0.7.2-beta` 指向包含本说明的发布提交；GitHub 预发布 Release 包含安装包与 `.sha256` 文件。

## 版本: v0.7.1-beta（开发候选，未发布）

这是实时双语草稿的显示与调度修复版，不改变旧识别调优模式和终句定稿逻辑。

### 实时草稿修复

- 英文中间识别结果若为全大写，显示层会改成句子式大小写，提高阅读性；混合大小写和最终识别证据不会被改写。
- 修复 140ms partial 持续重置 180ms 翻译防抖，导致连续说话时草稿翻译长期无法启动的问题。
- 调度改为“首次变化启动、后续变化合并”的节流；翻译仍保持最短 450ms 间隔，完整终句始终优先。
- 当原文只是向后增长或修订末尾时，已完成的译文可作为稍微滞后的可见结果保留，新译文完成后再原位替换；不兼容的过期结果仍会丢弃。

### 验证

- 正式构建门禁为 `262 passed, 7 skipped`；新增连续 partial 翻译调度、滞后译文安全性、英文大小写与 Pipeline 集成覆盖。唯一 warning 为声卡闭环测试的录音数据 discontinuity，该测试通过。
- 候选安装包 `VoxSub-Setup-0.7.1-beta.exe` 为 214,877,960 字节（204.92 MiB），SHA256 `64B54C296C3CF5A53BB867889DA9CF479373BD79933E1377EB70C62BCEDC2C0B`。
- 主程序与安装包均使用 `CN=VoxSub Dev (self-signed)` 开发者自签名；签名状态为 `UnknownError`，不代表 Windows 信任或正式 OV 签名。
- 打包目录使用隔离配置启动 12 秒未提前退出；日志确认 `ui=0.7.1-beta core=0.7.1-beta`、窗口组件创建完成并进入事件循环。
- 修复提交 `8577608` 已推送 GitHub `main`；未创建 GitHub Release，等待用户验证。

## 版本: v0.7.0-beta（开发候选，未发布）

这一版把本地 TTS 正式接入模型广场和运行中设置，同时修复实时双语草稿使用时朗读开关看似无效的问题。

### 朗读模型广场

- 新增 MeloTTS 中英双语自然音色、Icefall AISHELL3 中文轻量和 Icefall LJSpeech 英文轻量三个官方 sherpa-onnx 模型。
- 模型广场新增“语音朗读”筛选；下载、取消、断点续传、完整性校验、选择和卸载保护沿用现有模型管理逻辑。
- 旧版 `models/tts/zh` 与 `models/tts/en` 目录自动兼容，已有模型不会被误判为未安装。

### 设置与实时朗读

- 设置 → 语音朗读可分别切换中文和英文模型，只显示已安装且语言兼容的音色。
- 朗读开关和模型切换会立即传递给正在运行的 Pipeline，不再要求停止任务或重启 App。
- 实时双语草稿和智能上下文均可使用 TTS；草稿译文会持续变化，因此只朗读已经定稿的最终译文，避免重复和相互覆盖。
- 若后台 worker 启动时模型尚未安装，安装完成后会在下一个终句重新探测并加载，字幕流程始终不受阻断。
- 诊断中心按当前选中语言和模型进行 TTS 冒烟，不再随机拿到不匹配的音色造成误报。

### 验证

- 正式构建门禁为 `256 passed, 7 skipped`；唯一 warning 是声卡闭环测试报告一次录音数据 discontinuity，测试本身通过。
- 真实旧版中文/英文 TTS 模型分别合成 51,050 和 46,796 个 16 kHz `float32` 采样点，健康状态为 `ok`。
- 候选安装包 `VoxSub-Setup-0.7.0-beta.exe` 为 214,865,840 字节（204.91 MiB），SHA256 `252DE9C8881D1E268DA6053158F01368539B56D7A86902DDAE30D0E089F46D20`。
- 主程序与安装包均使用 `CN=VoxSub Dev (self-signed)` 开发者自签名；签名状态为 `UnknownError`，不代表 Windows 信任或正式 OV 签名。
- 打包目录使用隔离配置启动 12 秒未提前退出；日志确认 `ui=0.7.0-beta core=0.7.0-beta`、窗口组件创建完成并进入事件循环。
- 功能提交 `8dd5c4a` 已推送 GitHub `main`；当前不创建 GitHub Release，等待用户验证。

## 版本: v0.6.0-beta（开发候选，未发布）

这一版继续完善智能上下文的实时呈现，不改动其他识别调优方式的原有逻辑。

### 当前句动态草稿

- 主窗口和浮窗各只维护一条当前句草稿；流式识别每次产生新假设时原位替换，不把 partial 写入会话历史。
- Zipformer 在智能上下文模式下把 partial 采样间隔从 360ms 缩短到 140ms，并且只在识别文本真的变化时刷新，呈现更接近逐词字幕。
- 修复 Qwen3/Fun-ASR/SenseVoice 和云 STT 选择下没有动态草稿：内置 Zipformer 现在作为低开销流式旁路提供临时原文，当前生成式或云端模型继续负责最终识别和纠偏定稿；旁路不可用时安全回落到终句显示。
- 识别调优新增独立“实时双语草稿”开关，默认开启；关闭后不加载草稿旁路、不请求临时翻译，但智能断句、保守纠偏和语气词清理保持生效。
- partial 也会经过与终句一致的保守 hotword 纠偏和轻度语气词清理，但不会修改上下文历史或原始识别证据。
- 完整终句翻译完成后才提交为历史行；下一句若已开始识别，会在终句提交后立即恢复当前草稿，避免原译错位。

### 动态译文并发策略

- 智能上下文模式会对当前草稿生成临时译文；180ms 防抖、最短 450ms 请求间隔，且任意时刻只保留最新 revision。
- 终句翻译始终优先；草稿翻译较慢返回时会核对 revision，过期结果直接丢弃，不能覆盖后续原文和译文。
- 翻译器仍由同一工作线程串行拥有，避免本地/云翻译后端被并发调用；其他调优模式不启用动态译文请求。
- 生成式本地 ASR 与当前云 STT 客户端仍保持整段终句接口；动态草稿来自本地流式旁路，因此不会每个词都重新运行大模型或重复上传音频。

### 验证

- 新增独立 `LiveDraftState` 状态模型和 stale-result、终句优先、双语草稿 UI、partial 纠偏测试。
- 源码与正式构建门禁均为 `251 passed, 8 skipped`；唯一 warning 是声卡闭环测试报告的一次录音数据 discontinuity，测试本身通过。
- 真实 Qwen3 ASR + Zipformer 旁路 + VAD 音频链路产生 7 次草稿更新后正常提交终句；开关开启、关闭以及非智能上下文旁路均有自动化覆盖。
- 候选安装包 `VoxSub-Setup-0.6.0-beta.exe` 为 214,819,528 字节（204.87 MiB），SHA256 `585116D524FD75BD9E672339FCF04CC681F9FFF9BC3E6F1B91E3BF8FBAC6E2D2`；当前证书库无可用 VoxSub 代码签名证书，因此成品未签名。
- 打包目录使用全新隔离配置启动 12 秒未提前退出；日志确认 `ui=0.6.0-beta core=0.6.0-beta`、窗口组件创建完成并进入事件循环。
- 修复提交 `a7c29f3` 与开关提交 `0e26367` 已推送 GitHub `main`；本版本不会在用户验证前创建 GitHub Release。

## 版本: v0.5.0-beta（2026-08-24，已发布）

这一版把“智能上下文”作为新的识别调优方式加入 App。自动、响应优先、均衡、准确优先和自定义模式仍按原逻辑运行，不会被新算法隐式改变。

### 智能上下文断句

- 设置 → 识别模型调优新增“智能上下文（动态断句）”预设，以及上下文最长等待、保守纠偏和语气词清理三个独立选项。
- 对流式 Zipformer，达到原静音阈值时先判断当前文本是否明显未完；只有未完句会继续等待，语义等待到达硬上限后一定提交。
- 对生成式本地 ASR 和云 STT，声学片段仍由原 VAD 产生，再经独立、有界的上下文队列合并未完片段；上下文处理不会堵塞音频采集。
- 最长等待从首个待定片段开始计时，新片段不能不断延长截止时间；停止任务时会安全提交尾句。
- 临时字幕会展示“已稳定上下文 + 当前识别片段”，最终稳定后再送入现有翻译队列。

### 纠偏与语气词

- 保守纠偏只信任用户填写的常用词/专有名词，以及最近上下文中多次稳定出现的词；每段最多做少量可记录替换，不自由改写句子，也不补写数字、人名、否定或未识别内容。
- 轻度语气词清理只移除独立或带停顿的“嗯、啊、呃、额、唔 / um、uh、erm”等填充词；有语义的句尾语气保持原样，设置为关闭则完整保留原话。
- 每个稳定段同时保留原始识别文本和纠偏记录用于诊断；翻译和字幕使用稳定后的文本。

### 兼容性与验证

- 配置 schema 升级到 v2；旧配置自动补默认值，旧识别调优模式不经过上下文阶段。
- 上下文队列有明确容量和过载错误，后台线程结束顺序保持“识别 → 上下文 → 翻译”。
- 新增语义边界、合并、硬超时、保守纠偏、语气词开关、配置、界面和旧模式旁路测试。
- 源码全量回归：`243 passed, 8 skipped`；`compileall`、架构复杂度/队列边界守卫和 `git diff --check` 通过。
- 源码提交 `db74707` 已推送到 GitHub `main`；构建门禁再次通过 `243 passed, 8 skipped`。
- 本地候选安装包：`VoxSub-Setup-0.5.0-beta.exe`，214,795,950 字节（204.85 MiB）；SHA256 `38CF47DE43CB39B45BAF8241464A7C06B5AEF6AF28CE15FF76E7B32063047EA8`。
- 当前用户证书库没有可用的 VoxSub 代码签名证书，因此本次候选安装包和主程序未签名；未擅自生成新证书或更换开发者身份。
- 打包成品使用隔离配置启动 12 秒未提前退出；日志确认 `ui=0.5.0-beta core=0.5.0-beta`、窗口组件创建完成并进入事件循环。
- 标签 `v0.5.0-beta` 指向提交 `0791575`；GitHub 预发布 Release 已创建，安装包与 `.sha256` 均已上传并由 GitHub 复核大小和摘要。

## 版本: v0.4.2-beta（2026-08-24，已发布）

这一版集中修复长期运行和后续扩展中的结构性风险，不改变现有模型目录和用户配置的兼容路径。

### 稳定性与可维护性

- 配置加入统一 schema、版本迁移、类型/范围/BaseURL 校验和并发写保护；损坏或旧版配置会安全回落。
- 采集、按应用音频回调、识别、翻译和 TTS 队列全部设置容量与明确过载策略，慢后端不再无限占用内存。
- TTS 设置接入独立合成/播放 worker；失败时降级为仅字幕，不阻塞识别或翻译。
- 配置、模型清单、模型广场状态、字幕和二进制模型提交统一使用原子写入；下载文件必须满足尺寸/SHA 证据才会提升为正式文件。
- Pipeline 引入显式生命周期状态，并拆出文件转录、字幕导出和实时组件构建；UI 会话、字幕历史和调优草稿由独立状态对象持有。
- 下载、硬件探测、llama 运行时选择和启动计划按职责拆分；Intel NPU 的驱动门槛、真实推理探针和失败降级保持不变。
- 新增自动架构守卫，阻止核心层反向依赖 UI、无界生产队列和分支复杂度达到 15 的函数重新进入代码库。
- 修复“设置 → 外观”中译文字号和浮窗不透明度的上箭头无法点击；两项改用与识别调优一致的独立上下箭头和命中区域。

### 候选验证状态

- 源码全量回归：`229 passed, 8 skipped`。
- `compileall` 与 `git diff --check` 通过。
- 本地候选安装包：`VoxSub-Setup-0.4.2-beta.exe`，214,762,511 字节（204.81 MiB）；SHA256 `9B691BF2FE6B5F9F3AD7C0B53CB936547D38550CC894BDC8FAA43684770E99D6`。
- 当前用户证书库没有可用的 VoxSub 代码签名证书，因此本次候选安装包和主程序未签名；未擅自生成新证书或更换开发者身份。
- 打包成品使用隔离配置启动 12 秒未提前退出；日志确认 `ui=0.4.2-beta core=0.4.2-beta`、窗口组件创建完成并进入事件循环。
- 标签 `v0.4.2-beta` 指向候选归档提交 `d912f8e`；GitHub 预发布 Release 已创建，安装包与 `.sha256` 均已上传并由 GitHub 复核大小和摘要。

## 版本: v0.4.1-beta（2026-08-21 重打包）

这一版重点解决模型文件在升级后难以管理的问题，并改善了全屏时打开应用内页面的体验。

### 模型保存与迁移

- 新安装默认把模型放在安装目录下的 `Models` 文件夹，并按 `stt`、`translate`、`vad`、`tts` 用途整理。
- 升级用户会继续使用原来的模型目录，不会因为安装新版而被迫重新下载模型。
- 设置新增模型保存位置管理，可以把整个模型库迁移到其他磁盘，也可以从其他目录手动导入已有模型。
- 迁移采用合并方式，目标目录已有同名文件时保留双方，不覆盖用户数据；未完成的下载进度也不会被误当成完整模型。
- 安装器只创建可写的 `Models` 目录，不包含删除模型目录的卸载规则，升级不会清空模型。

### 更新说明与全屏

- 新版第一次启动时显示一份面向普通用户的更新说明，同一版本之后不再重复弹出。
- 设置 → 关于中保留历史版本的更新说明，方便之后查看。
- 主窗口处于全屏时打开设置或模型广场，页面切换和返回都会保持全屏。

### 本次问题修复

- 修复识别调优数字框上方调节按钮无响应的问题，现在加大和减小都能正常点击。
- 修复迁移模型和更改保存位置时可能卡死、关闭页面导致崩溃的问题；大文件迁移始终在后台进行，迁移未完成时不会销毁工作线程。
- 修复升级后模型根目录已经切换、但旧目录中的翻译模型不再显示的问题。应用会兼容查找旧根目录，用户不需要重复下载。
- 修复模型迁移后诊断仍按旧清单误报文件缺失，以及开始识别时 Pipeline 仍读取旧目录的问题；旧清单会自动同步到新用途目录，停止状态下切换目录会清理旧模型缓存。
- 改善 Teams 新版的按应用捕获：选择 Teams 或其会议窗口时会自动使用宿主进程，会议声音所在的 WebView2 子进程也会被包含。

### 字幕浮窗

- 浮窗工具栏新增“仅原文 / 仅译文 / 对照翻译”显示模式，选择会自动保存。
- 可以分别调节浮窗边框到文字的距离，以及原文和译文之间的距离；紧凑设置不再保留大块无用空白。
- 长句不再自动改变浮窗宽高。字幕会在固定尺寸内换行，使用普通滚轮查看当前长句，使用 `Ctrl + 滚轮`翻看字幕历史。
- 未锁定时可以自由调整浮窗宽高；锁定并开启点击穿透后，所有编辑按钮隐藏，只保留悬停“解锁”。
- 工具栏展开时字幕会临时避让，关闭工具栏后恢复用户设置的内容边距，不会遮住第一行字幕。

### 验证与发布

- 本版安装包：`D:\OneDrive\app_dve\Release\VoxSub-Setup-0.4.1-beta.exe`（214,738,176 字节，204.79 MiB，VoxSub 开发者自签名）。
- SHA256：`D1244780331B124381E4BBB354983B2A287D522060699DC9D24D26E3CF63CC1A`；同名 `.sha256` 文件已生成。
- 回归验证：`199 passed, 7 skipped`；另有 1 条声卡驱动数据不连续的非阻塞警告。
- 本版仍为开发测试版，Intel NPU 的已知边界与上一版保持不变：Hy-MT2 7B 精确权重仍未在 Intel NPU 真机完成验证，sherpa-onnx ASR 和 OPUS 运行时也不支持 NPU。

## 版本: v0.4.0-beta（2026-08-20）

这一版把已在 Intel NPU 真机跑通的翻译链路正式装入安装包，同时为尚未取得权重实测的 7B 模型建立保守的探针与降级边界，并修复大模型下载中途断流后被误判完成的问题。

### Intel NPU 调度与运行时

- 在 Intel AI Boost（驱动 `32.0.100.4841`）上，Hy-MT2 1.8B Q4/Q6/Q8 已通过自动设备调度和禁用 CPU 回退的强制 NPU 推理。
- 安装包固定携带该次验证通过的 `llama.cpp b10470 + OpenVINO 2026.2.1` no-NPUW 运行时；构建机无缓存时可从固定版本源码重建。
- NPU 固定单并发、最多 1024 context 和无状态执行，并启用 OpenVINO 编译缓存；健康检查后还会执行真实短翻译，不能产出有效译文就淘汰该后端。
- Hy-MT2 7B Q4/Q6/Q8 根据 llama.cpp OpenVINO 对 Intel NPU、相同量化格式及相近 7B/8B GGUF 的公开支持列为“NPU 待验证”，不标为实测可用。
- 加速后端在启动或处理当前句时失败，会立即尝试下一候选；NPU 失败后根据核显兼容性、内存余量和 CPU 能力选择核显或 CPU，并记录候选评分和原因。
- 修正 OpenVINO GPU 被错误归类为 CPU 的显示与路由问题。

### 大模型下载可靠性

- 下载器现在用目录声明的精确文件大小识别 CDN 提前断流，不再把不完整的 7B Q8 文件提升为已安装模型。
- 每个下载源最多自动重试三次，并在切换全球/中国源时继续复用 `.part` 进度。
- 旧版已经误保存为目标文件的不完整大模型会自动迁回断点文件继续下载；最终文件必须同时通过大小与 SHA256 校验。

### 验证与发布

- 全量回归：209 passed / 2 skipped。
- PyInstaller onedir 成品约 802.4 MiB；隔离配置启动 8 秒未提前退出，日志确认 `ui=0.4.0-beta core=0.4.0-beta` 并进入事件循环。
- NPU 运行时关键文件与 Intel 真机验证产物 SHA256 逐项一致。
- 发布包：`VoxSub-Setup-0.4.0-beta.exe`，204.73 MiB，开发者自签名。
- SHA256：`408AE75789EDDD880BF1A50976363CA27564C80D27988382EA6B5AE887BDDFCA`；同名 `.sha256` 文件已生成。

### 已知边界

- Hy-MT2 7B 尚未使用精确权重在 Intel NPU 真机完成验证；“NPU 待验证”代表公开资料支持尝试，不代表保证可运行。
- sherpa-onnx ASR 与内置 OPUS 翻译仍不支持 Intel NPU，会按各自运行时能力使用独显、核显或 CPU。

## 版本: v0.3.9-beta（2026-08-19）

这一版完成了主窗页面架构重构，并修复了内置模型无法通过“修复安装”恢复的问题。

### 内置页面与 UI

- 设置页和模型广场改为主窗内置页面，加入遮罩、背景模糊、返回导航和设置 → 模型广场的页面历史；诊断与日志继续保留独立弹窗。
- 设置页关于区域加入 GitHub 链接；保留中英文 UI 同步和设置草稿保存/放弃边界。

### 内置模型修复

- 内置 OPUS-MT 现在精确检查两个语言方向的编码器、解码器、配置和 tokenizer 文件。
- 内置 Zipformer 现在同时检查 `tokens.txt`、encoder、decoder 和 joiner ONNX 文件。
- “修复安装”优先从 Hugging Face/HF 镜像、GitHub/ModelScope 下载缺失资源，已有文件不会重复下载；在线源全部失败后才提示使用安装包修复。

### 已知问题

- Intel NPU 调度仍未完成。应用可能检测到 Intel NPU，但当前版本不能保证模型实际使用 NPU，推理可能回退到 GPU 或 CPU；该问题将在后续版本继续排查。

### 验证与发布

- 全量回归：190 passed / 3 skipped。
- 发布包：`VoxSub-Setup-0.3.9-beta.exe`，205.15 MiB，未签名。
- SHA256：`D500E7045B503C58F13C81ECCA37675205097157F13CB48B088ED089A4182F29`；同名 `.sha256` 文件已生成。
- PyInstaller onedir 成品约 801.2 MB，包含 VAD、FFmpeg 及 CPU/Vulkan/OpenVINO llama.cpp 运行时。

## 版本: v0.3.8-beta（2026-08-19）

这一版统一了所有选择类控件的视觉状态，并让安装向导按 Windows 界面语言自动显示。

### UI 选择控件

- 设置页所有 STT、翻译档位和主题单选项统一使用自绘圆环 + 圆心，不再在 Windows Qt 选中后变成方形。
- 同时录音、TTS、浮窗锁定和调试模式统一为圆角轨道开关；模型广场的任务筛选统一为稳定的胶囊按钮。
- 新增公共选择控件模块和回归测试，后续新增选择项不会再回到平台原生绘制。

### 安装语言

- 安装器支持英文、简体中文和繁体中文。
- 默认关闭语言选择弹窗，按 Windows UI 语言自动匹配；没有对应翻译时回退英文。

### 验证与发布

- 全量回归：181 passed / 3 skipped。
- 发布包：`VoxSub-Setup-0.3.8-beta.exe`，205.11 MiB，未签名（本机构建环境无可用代码签名证书）。
- SHA256：`A8B2D9AD82A7544033F6640116F5E783848F3F49EAB0A43DB55A6B4EA32BA99F`；同名 `.sha256` 文件已生成。
- 成品启动冒烟：隔离配置下日志确认 `ui=0.3.8-beta core=0.3.8-beta`，事件循环正常启动。

## 版本: v0.3.7-beta（2026-08-18）

这一版把云端语音识别和云端翻译拆成两个独立可组合的阶段，并修复云翻译默认超时会被错误缩短的问题。

### 云 STT、云翻译与混合模式

- 设置页新增本地/云 STT 选择，与本地快档/质量档/云翻译独立组合；会显示当前本地、云端或混合模式摘要。
- 云 STT 与云翻译分别保存 API Key、BaseURL 和模型名。旧版单一云配置自动迁移到翻译侧，已有用户不会丢失原先的云翻译设置。
- 支持四条链路：本地 STT + 本地翻译、云 STT + 本地翻译、本地 STT + 云翻译、云 STT + 云翻译。
- 云 STT 使用 OpenAI 兼容的 `/v1/audio/transcriptions` 接口。采集、VAD、句段边界均在本机运行，只把完整语音片段以 WAV 上传；识别请求在独立队列执行，不阻塞录音线程。
- 云翻译与云 STT 均统一规范化 BaseURL，避免输入已包含 `/v1` 时形成 `/v1/v1/...`；云翻译默认超时恢复为正确的秒级数值。

### 验证与发布

- 全量回归：177 passed / 3 skipped。
- 成品启动冒烟：隔离配置下运行 8 秒未提前退出。
- 发布包：`VoxSub-Setup-0.3.7-beta.exe`，205.05 MiB，未签名（本机构建环境无可用代码签名证书）。
- SHA256：`701E1AC4C2188629D503318A7F8EE758C5D53BF9149051D51BA6E523AF4AE698`；同名 `.sha256` 文件已一并生成。

## 版本: v0.3.6-beta（2026-08-18）

这一版修复新设备首次使用失败，以及所有文本导出可能卡住界面的问题。

### 新设备可用性

- 安装包附带经过 SHA256 校验的 Silero VAD 基础模型（2.3 MB）；首次运行会自动复制到当前用户的模型目录。
- 实时 Pipeline 改为事务式构建：ASR、VAD、分句器全部初始化成功后才提交。缺 VAD 等失败不再留下半初始化 ASR，重试启动不会触发 `NoneType.feed` 或残留采集线程。
- 诊断 VAD 冒烟检查也会尝试修复随包附带的基础模型，并给出重新安装当前版本的明确提示。

### 导出可靠性

- 会话、日志和诊断报告不再调用 Windows 原生保存窗口，统一改为绑定当前 VoxSub 窗口的 Qt 保存对话框，避免保存窗口隐藏在后方而误以为应用卡死。
- 生成文本与文件写入移至后台线程；导出期间按钮显示进行状态，完成后恢复并报告实际路径。
- SRT、WebVTT、TXT 和诊断文本使用同目录临时文件加原子替换，防止中断时留下半个导出文件。

### 验证与发布

- 全量回归：164 passed / 3 skipped。
- 发布包：`VoxSub-Setup-0.3.6-beta.exe`，205.09 MiB，未签名（本机构建环境无可用代码签名证书）。
- SHA256：`EA7B3BD9F73DD97AC876723F8B90CB2726F6843B97A39F06F7973C8807B9A57E`；同名 `.sha256` 文件已一并生成。

## 版本: v0.3.5-beta（2026-08-18）

这一版修复翻译档位的选中控件，并扩充模型广场的可调用模型档位。

### 设置界面

- 翻译档位与外观主题的单选控件改为稳定的“细圆环 + 圆心点”绘制，不再因 Windows Qt 选中伪状态而从圆形变为圆角方块。
- 自绘单选控件显式跟随浅色、深色和系统主题，避免仅改选中态后产生文字对比度错误。

### 模型广场

- 新增 SenseVoice Small INT8：中文、粤语、英语、日语、韩语的低资源多语种 ASR 选项；在 VAD 句界后使用 sherpa-onnx 离线识别器解码。
- Hy-MT2 1.8B 与 7B 新增 Q6_K / Q8_0 GGUF 档位。Q4 保持均衡默认，Q6 适合更高保真需求，Q8 只向内存和显存充裕的设备推荐。
- 目录现有 11 个模型选择，按任务内质量排序，并继续按独显 GPU → NPU → 核显 → CPU 计算推荐等级。

### 验证与发布

- 全量回归：158 passed / 3 skipped。
- 离屏截图确认质量档单选控件的选中态为圆环加圆心，模型广场首屏可正常展示新增目录。
- 发布包：`VoxSub-Setup-0.3.5-beta.exe`，203.37 MiB，未签名（本机构建环境无可用代码签名证书）。
- SHA256：`BD2B56302ABFF06DE6E2FE3DAAB8B4916D49C5681E3E1E050699D0F0B5FAC2FF`；同名 `.sha256` 文件已一并生成。

## 版本: v0.3.4-beta（2026-08-18）

这一版集中优化主界面、设置页和所有次级窗口的 Soft Premium 视觉一致性。

- 主窗移除可见的浮窗调整区，浮窗控制保留在浮窗工具条和设置页，主工作区只保留会话保存/清空操作。
- 设置页增加统一页头、可滚动页签和浮窗显示设置；识别调优、设备、外观等内容在较小窗口中不再被截断。
- 诊断中心增加自检摘要、重新检查入口、统一状态卡和更清晰的实时日志工具栏。
- 模型广场统一卡片间距、下载状态、推荐排序提示和操作按钮尺寸；下载进度条不再重复显示百分比。
- 公共主题补齐数值输入、下拉菜单、滚动条、主次按钮、信息提示和浮窗控制岛的状态样式。
- 浮窗控制工具条改用共享主题令牌，字号、不透明度和锁定状态继续保持即时生效。
- 全量回归：154 passed / 3 skipped。
- 发布包：`D:\OneDrive\app_dve\Release\VoxSub-Setup-0.3.4-beta.exe`（203.41 MiB）。安装包包含 CPU/Vulkan/OpenVINO 三套 llama.cpp 后端和 ffmpeg；当前未签名，因为构建环境没有可用的代码签名证书。
- SHA256：`FF08F72ECE76AE6D8B0A7AA555A6572D55FB2FB9A0386E8C37CFBF1D5BDE3827`。

## 版本: v0.3.3-beta（2026-08-18）

这一版修复 v0.3.2 浮窗验收遗漏的两个底层问题。

- 字号：应用级 `QWidget { font-size: 14px; }` 曾覆盖字幕标签的 `setFont()`，导致数字变化但实际文字不变。字幕原文与译文现在使用局部字号样式，真实字体度量会随字号改变。
- 解锁：解锁时先清除 `WA_TransparentForMouseEvents`，再清除 `WindowTransparentForInput`，确保 Windows 原生 `WS_EX_TRANSPARENT` 位也被移除，恢复按钮点击、文字选择与窗口拖动。
- 回归门禁新增实际字体高度变化，以及解锁后 Qt 窗口标志和鼠标透明属性均已清除的断言。
- 全量回归 153 passed / 3 skipped / 0 failed；唯一警告来自既有真实声卡数据不连续检测，不涉及本次浮窗代码。
- 真实 Windows 输入验收：译文字体高度随 20→30 从 34px 增至 51px；通过锁定控制岛解锁后，操作系统鼠标可再次点击 `A+`（20→22）并拖动浮窗（窗口原点从 `(290, 280)` 移至 `(390, 340)`）。
- 打包后使用隔离配置启动 `dist` 成品，主界面与设置页正常；关于页显示 `ui 0.3.3-beta · 核心包 0.3.3-beta`。
- 安装包：`D:\OneDrive\app_dve\Release\VoxSub-Setup-0.3.3-beta.exe`（203.41 MiB）；主程序与安装包均由 `CN=VoxSub Dev (self-signed)` 签名。
- SHA256：`A4C527FCF71D2A916E05F61DC32A5F763ED91328CEF110C0885EDCB5BC14309B`。

## 版本: v0.3.2-beta（开发候选，2026-08-18）

这一版聚焦于用户实测暴露的浮窗交互、调优保存和启停卡顿。

### 浮窗

- 锁定后的字幕主体仍保持 Windows 原生鼠标穿透，同时使用独立顶层控制岛：鼠标移入浮窗即显示字号和解锁按钮，不再需切回主窗。
- 重建悬停工具条的固定尺寸布局，字号加减会立即更新文字、数值与配置，并为 Windows 字体度量预留宽度避免裁切。

### 识别调优

- 停顿断句范围扩展为 50–5000ms，最长单句 1–120s，VAD 阈值 0.01–0.99，候选数 1–16，最大文字量 32–4096；语速快、照稿念的视频可从 50–150ms 起试。
- 调优页改为明确的草稿事务：只有“保存调优”才落盘，“放弃更改”或直接关窗都恢复上次已保存值。
- 所有 `i` 说明改为鼠标悬停小气泡，点击不再弹出系统对话框。

### 响应与稳定性

- 模型配置/加载与 Pipeline 启动、停止、尾句冲刷移到后台工作线程，主按钮立即进入“正在启动/正在结束”状态，Qt 事件循环不再等待最多数个线程的 join。
- 结束期间保存/清空/切换模式暂时禁用，待剩余音频和字幕整理完成后再开放，修复“结束后立即保存”卡死。

### 验证与构建

- 全量回归 153 passed / 3 skipped / 0 failed；包含慢启动/停止不堵塞 UI、结束后保存串行化、浮窗原地解锁/字号、按钮无裁切与调优事务回归。
- 打包前完成真实 Windows UI 点击验收：主窗与浮窗字号按钮完整显示且生效；锁定控制岛可调字号并原地解锁；断句 50ms 可输入；未保存关窗不落盘、显式保存正确落盘。
- 打包后使用隔离配置启动 `dist` 成品，主界面、识别调优页和关于页均正常；关于页显示 `ui 0.3.2-beta · 核心包 0.3.2-beta`。
- 安装包：`D:\OneDrive\app_dve\Release\VoxSub-Setup-0.3.2-beta.exe`（203.37 MiB）；主程序与安装包均由 `CN=VoxSub Dev (self-signed)` 签名。
- SHA256：`1F480C61DEC75D0B7C5676961054F5EEC09C10198CBF84504D01E470D75B1F25`。

## 版本: v0.3.1-beta（开发候选，2026-08-18）

这一版专门处理 Qwen3-ASR 的碎句、长延迟和实时队列丢音，并补齐录音、会话与浮窗交互。

### 识别与实时管线

- 生成式 ASR 改为 VAD 自然断句，再由独立识别线程解码；自动档保留 700ms 停顿上下文，不再每 3.2 秒硬切成“别。”“细胞。”。
- 音频、识别、翻译三段解耦；常规积压会显式缓冲，不再为追求“表面实时”而静默丢音。只在音频积压达 10 分钟的安全上限时明确停止并报错。
- 新增识别调优页：预设、语音灵敏度、停顿断句、最长单句、候选数、最大文字量和常用词。每个选项旁都有可悬停/点击的 `i` 通俗说明。

### 录音、会话与浮窗

- 麦克风同传可选同时录制 16kHz 单声道 WAV，操作为开始 → 暂停/继续 → 结束并保存；未开启时不落盘。
- 当前双语会话可清空或导出 TXT/SRT/VTT；主窗和字幕浮窗文字可直接选中复制。
- 修复浮窗字体构造错误导致的字号失效；加入类似桌面歌词的悬停工具条、字号加减、锁定/鼠标穿透与主窗解锁。
- 模型下载进度条不再内嵌易被裁切的百分比，数值仅保留在详情行。

### 验证边界

- 开发版全量回归 148 passed / 3 skipped；实机 Qt 界面已验收调优页及通俗说明弹窗。
- 本机 Qwen3-ASR 0.6B INT8 实际素材由原先重复 3.2 秒切片改为 12.02s + 5.04s 的语义分段，模型加载与两段识别无崩溃。
- v0.3.1-beta 已构建到 `D:\OneDrive\app_dve\Release\VoxSub-Setup-0.3.1-beta.exe`（203.4 MB）；主程序与安装包均由 `CN=VoxSub Dev (self-signed)` 签名。
- SHA256：`BF7D87AAEC8A98F4FFA570B6E8F09860097862847A1F43748881BE0883343A6B`。

## 版本: v0.3.0-beta（开发候选，2026-08-18）

这一版优先修复用户实测暴露的“开始后无字幕、识别慢且错、翻译输出解释文字、偶发闪退”，并加入面向大众硬件的模型广场。

### 核心修复

- ASR 与翻译改为独立工作队列，翻译慢时不再堵住音频识别；停止时由单一线程拥有和刷新分句器，消除并发 `flush()` 触发 sherpa 原生崩溃的路径。
- Zipformer 改为 encoder/joiner INT8 + decoder FP32、4 路 beam search、短句硬切与 360ms 局部字幕；本机 50.21 秒素材识别约 1.38 秒（RTF 0.028），信息完整度明显高于旧全 INT8 greedy。
- Qwen 旧质量档增加严格“只输出译文”提示、输出污染校验与一次重试；模型广场质量档改为 2026 Hy-MT2 1.8B/7B。
- 新增 Fun-ASR-Nano 2512 与 Qwen3-ASR 0.6B 的 sherpa-onnx 适配；生成式 ASR 只在句界执行，避免反复解码拖慢实时链路。

### 模型广场

- 按任务内质量分降序展示，只收录已有运行时适配且在资源/质量 Pareto 前沿仍有价值的模型。
- 根据 CPU 核心数、内存、GPU 与显存估算负载：深灰“不推荐”、黄“较为推荐”、绿“推荐”、红“满载”。质量明显落后或配置带不动均归入“不推荐”。
- 支持后台下载、取消、断点续传、SHA256、压缩包越界防护、切换和安全卸载；当前使用中与内置兜底模型禁止卸载。
- GitHub/Hugging Face 全球源与 ModelScope 中国源自动测速切换，也可手选。2026-08-18 已对目录内全部实际下载地址做 HEAD 检查，均返回 200。
- 硬件推荐与调用统一按独显 GPU → NPU → 核显 → CPU；构建固定打包 llama.cpp b10470 CPU/Vulkan/OpenVINO 三后端，Intel NPU 可走 OpenVINO，模型运行时不兼容时明确降级。

### UI

- 模型广场与主窗入口按 Hermes `ui-style-library` 的 Soft Premium 规范实现（VARIANCE=6 / MOTION=4 / DENSITY=3）。
- App 内实时日志继续使用内存处理器，不需要外部程序持续占用日志文件。

### 验证边界

- 已完成单元/集成回归与开发版 UI 冒烟；Fun-ASR/Qwen3-ASR/Hy-MT2 的运行时构造接口已验证。
- 新模型权重体积约 1–4.6 GB，本轮未替用户下载；首次下载后的真实语音/翻译质量仍需作为发布候选实机验收项。
- v0.3.0-beta 安装包已生成于 `D:\OneDrive\app_dve\Release\VoxSub-Setup-0.3.0-beta.exe`，SHA256 为 `A79FC784AC54848112897A193B94865A4C144816E462A4AF8F61D26E69E417E3`。

## 版本: v0.2.0-beta (2026-08-17)

用户测试修复版：打通采集设备到字幕 UI 的真实链路，并补齐文件导入、设备选择、指定应用内录与应用内日志。

### 功能
- **A 模式 · 麦克风同传**：实时识别说话 → 双语字幕（原文+译文）
- **B 模式 · 应用 / 系统声音字幕**：选择系统输出端点，或只抓取指定应用及其子进程的声音
- **C 模式 · 文件字幕**：UI 选择音频/视频 → 内置 ffmpeg 自动提音 → 分句识别 → 翻译 → 导出 .srt
- **双模翻译**：快档 OPUS-MT（~0.5s/句，离线）/ 质量档 Qwen-1.5B（~0.7s/句，离线，混合语言更准）/ 云端 API（可选，用户填 key）
- **全本地运行**：不联网可用；无输入音频落盘（隐私优先）
- **硬件自适应**：DirectML 自动用 GPU；有 NPU 的机器可路由
- **诊断中心**：自检 6 项、**实时日志页签**、运行时 DEBUG 开关；日志文件锁定时仍保留内存日志
- **设备设置**：麦克风、系统输出端点、可见应用窗口三类选择
- **UI**：Hermes Soft Premium（VARIANCE=6 / MOTION=4 / DENSITY=3），三档主题与托盘常驻

### 关键修复
- 音频设备 `start()` 失败不再静默杀死线程，主窗立即显示可处理的错误
- B 模式不再误选枚举列表第一个静音端点，默认匹配 Windows 当前默认扬声器
- Pipeline 后台回调通过 Qt Signal 回到主线程；首条实时字幕会自动显示字幕浮窗
- 增加单实例锁，避免两个进程同时占用音频设备与轮转日志

### 性能 (i5-13600KF / RTX 4060, CPU 推理)
- 识别：流式，边说边出
- 快档翻译 573ms/句 · 质量档 690ms/句（均 <1s）

### 安装
- 运行 `VoxSub-Setup-0.2.0-beta.exe`
- **模型需单独获取**（安装包不含 2.4GB 模型）：首次运行在"诊断页/设置"点下载，自动走 ModelScope 主源 + 断点续传 + SHA256 校验
- 需要微软 VC++ 运行库（安装器自动装）

### 已知问题 / 限制
1. **英文名 VoxSub 在 GitHub 有同名同类项目**（待用户确认保留/改名，见 STATUS.md）
2. self-signed 版 SmartScreen 会提示"已保护你的电脑"→ 点"更多信息"→"仍要运行"；正式版将用 OV 证书签名
3. 个别受 Windows 麦克风隐私策略限制的设备需要在“设置 → 隐私和安全性 → 麦克风”授权桌面应用
4. 指定应用声音依赖 Windows 10 2004 或更新版本；同一 PID 下多个窗口共享同一条进程树声音
5. 快档 OPUS 对中英混合句翻译质量有限（会灌音乐符号）——混合语言场景请用质量档
6. 语言对当前固定中⇄英，多语言（马来语等）在 roadmap

### SHA256（发布物校验）
> 安装包 SHA256 由构建脚本在签名后生成，用户可用
> `certutil -hashfile <file> SHA256` 自校验。

### 发布物（正式版统一输出到 D:\OneDrive\app_dve\Release，用户约定）

> 正式版安装包/发布物编译到 `D:\OneDrive\app_dve\Release`（2026-08-17 用户指定）。
> 每个正式版 = 安装包 + SHA256 + 签名 + 本文件更新。

**v0.2.0-beta 安装包 (`D:\OneDrive\app_dve\Release\VoxSub-Setup-0.2.0-beta.exe`)：**
```
SHA256: C0C5A65C5176BA86DA0150EB5B2A748E6F001FC7359CBDFF9B77D9424424E861
签名  : CN=VoxSub Dev (self-signed) + DigiCert RFC3161 时间戳
```

### Roadmap
- M9 正式发布：InnoSetup 安装包 + OV 证书签名 + 微软商店上架（$19/年）
- 语言对扩展（马来语等）· 多显示器字幕位置记忆 · 字幕时间轴微调
- 开机自启（托盘）· 热词表（人名/术语优先识别）
