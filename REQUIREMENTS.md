# 语幕 VoxSub —— 需求与范围 (REQUIREMENTS)

## 一句话

面向 Windows 10/11 大众用户的实时翻译软件：麦克风对话、会议/网课系统声音、本地视频/音频文件，实时生成双语字幕，默认全本地离线运行。

## 目标用户

无技术背景的普通 Windows 用户；一键安装包，无需 Python、无需显卡驱动、无需翻墙。

## 核心功能

### P0
- A 麦克风同传：边说边出双语字幕，可选语音朗读
- B 系统声音字幕：抓取系统输出声音（会议/网课/视频播放），实时双语字幕
- C 文件模式：导入视频/音频，提轨识别+翻译，导出双语 .srt/.vtt 与纯文本
- OCR 屏幕翻译：支持一次性框选截图并返回原文/译文，以及持续监视用户指定屏幕范围并在原文坐标原位覆盖译文；实时模式必须可暂停、显示原文、重选和结束
- STT 与翻译独立选择：本地 STT 或 OpenAI 兼容云 STT；本地快档（OPUS-MT）、本地质量档（模型广场 Hy-MT2）或云翻译自由切换，支持本地 STT + 云翻译、云 STT + 本地翻译等混合模式。两侧各自填写 API Key、BaseURL 和模型名
- 硬件自适应：启动枚举 CPU/GPU/NPU，按任务类型路由设备，实测计分，自动降级
- 安装/运行体检：装前环境快照、首次自检诊断中心（模型完整性/后端可用/冒烟测试）、模型缺失自愈下载（SHA256+断点续传，ModelScope/HF 双源）
- 语言对可配置（默认中⇄英，模型支持范围内可扩展）
- OCR 隐私与降级：像素不得落盘或上传；本地/云翻译沿用当前翻译配置，选择云翻译时只发送 OCR 后的文字。翻译失败保留识别原文，覆盖窗不得被自身后续采集形成反馈回路

### P1
- TTS 朗读（本地 Piper，中英）
- 字幕历史保存 / 导出纯文本
- 悬浮字幕窗样式设置（字号/颜色/透明度/置顶）
- 热词表（人名/专业术语优先识别）

### P2
- 马来语等多语言（按模型支持评估）
- 字幕时间轴对齐微调
- 多显示器字幕位置记忆
- 手写体、艺术字和竖排文字的专用 OCR 后端/模型包

## 不做的事（明确范围）

- 不做手机端 / 网页端
- 不自建云服务与账号体系
- 语音识别以当前句动态草稿和句子级定稿为准，不承诺每个声学 token 的逐词结果
- 不做模型训练/微调
- 不内置付费 API 额度（云 STT 和云翻译均必须使用用户自己的 key）
